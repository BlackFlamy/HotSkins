import { createTheme } from "@mui/material/styles"

const theme = createTheme({
	palette: {
		background: {
			default: "#20222A"
		},
		text: {
			primary: "#20222A",
			secondary: "#20222A80"
		},
		separators: {
			light: "#E0E0E0",
			dark: "#3D4554"
		},
		elements: {}
	}
})

export default theme
